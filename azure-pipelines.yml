trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: study_var

steps:
- task: CmdLine@2
  inputs:
    script: |
      echo "hello azure pipeline"
      az account show 
      echo $(System.DefaultWorkingDirectory)
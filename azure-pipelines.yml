trigger:
  - main

pool:
  vmImage: 'ubuntu-18.04'

variables:
- group: study_var

jobs:
  - job: Hello_Terraform
    timeoutInMinutes: 0
    container:
      image: 'hashicorp/terraform'
      endpoint: ""
    steps:
      - script: |
           terraform -version